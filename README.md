# Speech Services in the Web

A prototype application to use speech recognition services in the web.

It consists of:
* React web app, in the `webapp` folder.
* Node.js web server, in the `speechserver` folder.

